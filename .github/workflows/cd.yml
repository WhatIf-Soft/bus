# Deployment pipeline — to be configured

name: CD

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Status
        run: echo "CD pipeline not yet configured"
