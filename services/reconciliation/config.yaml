server:
  host: 0.0.0.0
  port: 4011

database:
  payments_dsn: postgres://busexpress:busexpress_dev@localhost:5433/busexpress_payments?sslmode=disable

jwt:
  secret: dev-secret-change-me

platform:
  fee_bps: 500   # 5 % platform commission

log:
  level: info
